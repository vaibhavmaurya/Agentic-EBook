# =============================================================================
# Agentic Ebook Platform V3 — Local Development Environment Template
# =============================================================================
# Copy this file to .env.local and fill in your values.
# .env.local is gitignored — NEVER commit it.
# =============================================================================

# AWS Credentials (dev account only)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=                        # only if using temporary credentials (SSO, assume-role)
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=

# DynamoDB
DYNAMODB_TABLE_NAME=ebook_platform

# S3
S3_ARTIFACT_BUCKET=ebook-platform-artifacts-dev

# Step Functions
STEP_FUNCTIONS_ARN=arn:aws:states:us-east-1:<account_id>:stateMachine:ebook-topic-pipeline-dev
STEP_FUNCTIONS_ENDPOINT=                  # leave blank to use real AWS; set http://localhost:8083 for local SF emulator

# Cognito
COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
COGNITO_CLIENT_ID=

# SES
SES_SENDER_EMAIL=noreply@yourdomain.com
OWNER_EMAIL=owner@yourdomain.com

# Secrets Manager (name of the secret holding the OpenAI API key)
OPENAI_SECRET_NAME=ebook-platform/openai-key

# Local API server (used by the Jupyter notebook)
ADMIN_API_BASE_URL=http://localhost:8000
PUBLIC_API_BASE_URL=http://localhost:8000

# Dev Cognito admin credentials (for notebook auth helper cell)
ADMIN_USERNAME=dev-admin@yourdomain.com
ADMIN_PASSWORD=
